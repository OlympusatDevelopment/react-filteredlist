
/**
 * Hook gets called whenever the main application state gets updated.
 * Useful for getting the filters' current queryObject or queryString.
 * Pagination can also be read here as well as the current Selected View
 * @returns {*}
 */
export default state=>{
    //console.log('CURRENT STATE HOOK',state)
}